[project]
name = "cli"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Daria S", email = "skoropad.daria@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "typer>=0.25.0",
]

[project.scripts]
cli = "cli.cli:app"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
